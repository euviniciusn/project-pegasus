{
	auto_https off
}

:80 {
	# ── API reverse proxy ──
	handle /api/* {
		reverse_proxy api:3000
	}

	# ── Health check ──
	handle /health {
		respond "OK" 200
	}

	# ── Frontend (Fase 6) ──
	# handle {
	# 	reverse_proxy frontend:5173
	# }
}
