import ConverterFlow from '../containers/ConverterFlow.jsx';

export default function ConvertPage() {
  return (
    <ConverterFlow
      mode="convert"
      title="Converter Imagens"
      subtitle="Converta PNG, JPG e WebP para qualquer formato"
    />
  );
}
